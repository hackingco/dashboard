{
  "name": "fly-swarm-orchestrator",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "shared/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "deploy": "turbo run deploy",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.38.0",
    "@types/node": "^20.0.0",
    "dotenv": "^16.3.1",
    "turbo": "^1.10.16",
    "typescript": "^5.0.0"
  }
}